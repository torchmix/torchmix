import { Callout } from "nextra-theme-docs"
import { Tab, Tabs } from "nextra-theme-docs"

# DropActivation (Feedforward)

Apply dropout after activation layer.

```python copy
Feedforward(
    dim=768,
    plugins=[
        DropActivation(p=0.1),
    ],
)
```

import { Callout } from "nextra-theme-docs"
import { Tab, Tabs } from "nextra-theme-docs"

# DropProjectionIn (Feedforward)

Apply dropout after first linear layer.

```python copy
Feedforward(
    dim=768,
    plugins=[
        DropProjectionIn(p=0.1),
    ],
)
```

import { Callout } from "nextra-theme-docs"
import { Tab, Tabs } from "nextra-theme-docs"

# DropProjectionOut (Feedforward)

Apply dropout after activation layer.

```python copy
Feedforward(
    dim=768,
    plugins=[
        DropProjectionOut(p=0.1),
    ],
)
```
